task: binary
behavior_field: user_history

embedding:
  default_emb_dim: 16
  fields: {}

mlp:
  hidden_dims: [256, 128]
  activation: relu
  dropout: 0.1
  batch_norm: false
  input_batch_norm: false

attention:
  enabled: false
